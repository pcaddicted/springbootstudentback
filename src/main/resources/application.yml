spring:
    datasource:
        url: jdbc:mysql://localhost:3306/springjpastudent?useSSL=false&serverTimezone=UTC
        username: root
        password: "woshiwenbo"
        driverClassName: com.mysql.cj.jdbc.Driver
    jpa:
        database: MYSQL
        show-sql: true
    #Hibernate ddl auto
        hibernate:
            ddl-auto: update
            naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5Dialect
